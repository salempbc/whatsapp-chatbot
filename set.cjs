const crypto = require('crypto'); require('dotenv').config(); const domain = 'https://whatsapp-chatbot-production-131e.up.railway.app'; const token = process.env.BOT_TOKEN; const secret = crypto.createHash('sha256').update('webhook:' + token).digest('hex').slice(0, 48); fetch('https://api.telegram.org/bot' + token + '/setWebhook', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ url: domain + '/api/bot-webhook', secret_token: secret }) }).then(r=>r.json()).then(console.log);
